"""Cardeep production pipeline: deterministic, cheap, scalable per-entity E2E.

Phases (docs/workflows/README.md): DESCUBRIR -> SCRAPEAR -> RECETA -> API -> BORRAR,
plus the transversal VERIFICAR (VAM). Implemented incrementally, gate by gate.
"""
