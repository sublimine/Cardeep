# Auditoría adversarial del proyecto — 2026-06-15

> Mandato: "revisar todos los puntos VERDES a nivel átomo + AUDITA TODO el proyecto
> internamente, verifica coherencia e lógica del negocio. No confiamos en ningún resultado."
> Método: 4 agentes adversariales en paralelo (verification-stack / E2E-pipeline / live-DB
> business-logic / API+migraciones), read-only, evidencia obligatoria. El Director re-verifica
> cada hallazgo antes de actuar. Este documento es el registro + el triaje.

## Triaje

| Estado | Significado |
|---|---|
| ✅ FIXED | arreglado + verificado + commiteado este turno |
| 🟢 €0-TODO | arreglable a €0 ahora (siguiente) |
| 🟡 HARVEST-GATED | se manifiesta/prueba solo con cosecha real (fase spend) |
| ⚪ BY-DESIGN / DATA | comportamiento correcto declarado, o marcador de fuente (no bug) |

---

## CRÍTICOS

- **B-1 — quórum VAM ficticio + deadlock de escritura** (3 agentes convergieron). `verify.py`
  escribía `verifier_paths` como strings e `independent_values` como dict → las columnas
  GENERATED del 0026 devolvían 0 → 989/991 TRUSTWORTHY con quorum_n=0, y un TRUSTWORTHY nuevo
  reventaba con CheckViolation (deadlock). **✅ FIXED** (`99306d8`): `_path_family` + decisión
  alineada al constraint (modal≥2 ∧ familias≥2 ∧ orígenes≥2) + INSERT array/objetos; verificado
  adversarial (db+http→TRUSTWORTHY 2/2/2, same-family→UNVERIFIED, divergent→REFUTED); +9 tests.
- **F2 — `verdict_audit` destruible con TRUNCATE** (el trigger de inmutabilidad es ROW-level
  UPDATE/DELETE; TRUNCATE no dispara row-triggers; `cardeep` es superuser). 🟢 €0-TODO: añadir
  `BEFORE TRUNCATE` trigger (migración).
- **F3 — hash-chain cubre 5/1044 verdicts** (el trigger `cdp_audit_append` se instaló en 0026;
  los 1039 verdicts pre-0026 no tienen audit row). ⚪ BY-DESIGN parcial: la cadena protege
  FORWARD desde 0026; backfill cronológico = opcional (los pre-0026 son baseline histórico). 🟡
  un backfill enhebrado es harvest/maintenance, no €0-urgente.
- **CRÍTICO-1 — COMPLETED inalcanzable** (`g5_delta` siempre False vs trigger exige g5=TRUE).
  ⚪ BY-DESIGN: G5 es el gate de 2ª-corrida harvest-gated; COMPLETED=0 a €0 es CORRECTO. Cuando
  G5 corra (harvest), COMPLETED se desbloquea. (Documentado en SU-B2.)
- **B-2 — gap 40.016 (B1 dealers) vs 61.551 (entity kind≠particular)** sin reconciliar en verdict.
  🟡 el B1 aplica filtros (sells_cars/status/dedup) no expuestos en verifier_paths; reconciliar +
  re-emitir B1 con paths ricos = harvest/re-VAM (depende del fix B-1 ya aplicado para futuros).

## ALTOS

- **H-3 — `/geo/{prov}/tree` cuenta 281K particulares** (sin `kind<>'particular'`), inconsistente
  con `/geo/{prov}/entities` (que sí filtra). 🟢 €0-TODO (API).
- **H-4 — `/geo/completeness` sin filtro status/kind** (incluye 329K particulares + 11K unverified).
  🟢 €0-TODO (API).
- **H-5 — `/platforms/{cdp}/inventory` sirve coches `gone`** (5 casos; falta `v.status='available'`).
  🟢 €0-TODO (API).
- **H-2(mig) — SHA256 drift en 0018/0026/0027/0028** (archivo≠ledger; editados post-apply). 🟢
  €0-TODO: investigar (¿cambio semántico o cosmético?) + reconciliar + `migrate verify`.
- **F4 — rama tolerancia daba TRUSTWORTHY sin modal** (`verify.py`). **✅ FIXED** con B-1 (drift_ok
  ahora exige top_n≥2).
- **F5 — hash-chain no fork-safe bajo inserts concurrentes** (SELECT prev sin FOR UPDATE). 🟡
  harvest-scale (45+ scrapers concurrentes); €0-fixable (FOR UPDATE) pero solo importa con cosecha.
- **F6 — `entity_field`/`delta` nunca alcanzan TRUSTWORTHY en L4** (Lens A sin handler). 🟡
  harvest-relevante (esos claims fluyen en harvest); el A-only ya da REFUTED honesto (safe).
- **F7/H-7(L3) — `gestion_item.verdict_id`→verification_verdict acepta quorum_n=0 como prueba de
  RESOLVED** (más débil que el claim de Inquisición que lo abrió). 🟢 €0-TODO menor (CHECK o FK).
- **E-2 / H-2(db) — Flexicar/OcasionPlus 7-14% captura** (cap 1000/entidad, 1 run). 🟡 HARVEST
  (re-correr con facet-partición). Ya REFUTED-marcado (honesto).
- **B-2b — denominador Chao2 REFUTED** (N=852k vs techo CNAE-451 87k). ⚪ BY-DESIGN: el F8
  CNAE-451 (94,3%) es el sello válido; Chao2 quedó REFUTED a propósito (capturas disjuntas).
- **B-3 — 596 desguaces sells_cars=true con 0 inventario**. 🟡 semántica de negocio (desguace=
  despiece, no venta de coche entero) — decisión de modelo, harvest-relevante.
- **H-8(db) — harvests incompletos con error** (motor.es column-error, coches.com unicode,
  autocasion timeout, ocasionplus HTTP500). 🟡 HARVEST (re-lanzar con fix).

## MEDIOS / BAJOS (resumen)

- MVCC: `ingest.py` UPDATE incondicional de `last_seen` (dead tuples), ON CONFLICT sin threshold
  temporal. 🟡 harvest-scale (importa con 300+ scrapers).
- `discover.py` ON CONFLICT silencia enriquecimiento (phone/web/lat/lon no se actualizan). 🟡
  harvest (degrada dedup).
- `diff_vehicle`: NULL en old/new suprime PRICE/KM event; sin REACTIVATED event. 🟡 harvest.
- Outliers de DATOS (marcadores de fuente, NO bugs de código): 68 precios >5M, 810 km >2M,
  11.718 precios <100€, 2.459 km=0, 13 años >2026. ⚪ DATA — son marcadores Wallapop/Milanuncios
  ("a convenir"/"no declarado"); un detector de saneo (price_trap/fabrication ya existen) los
  cuarentena en harvest. NO se inventan; se cuarentenan.
- Geo: 87,7% sin lat/lon (mayoría particulares), 6.676 dealers sin municipality_code (ciegos a
  dedup), 43 fuera del bbox España. 🟡 DATA-BLOCKED €0 (necesita geocoding/INE) — ya en SU-A6.
- `sells_cars=NULL` en 7.201 garajes; `platform_listing.removed_at` nunca poblado. 🟡 harvest.
- codes.py: province_code erróneo → 2 cdp_code (canonical_key no incluye province en el hash). 🟢
  €0-TODO menor (mitigado por dedup deep_link 0027).
- API: `/health` expone métricas sin auth; rate-limit por IP (proxy-header); cache-key sin API-key
  (multitenancy futuro); `/sources` sin paginación (47 filas). 🟢 €0-TODO menores.

## LIMPIOS verificados (no tocar)

Integridad referencial total (0 orphans: vehicle/entity_source/entity_completion/vehicle_event/
platform_listing); cdp_code 390.621 únicos 0-null determinista; vehicle_event append-only; deep_link
0 NULL en 1.689.243 disponibles; envelopes API 100% consistentes; 429 envelope; TTLCache acotada;
cache no guarda errores; 0 SQL-injection (todo parametrizado); 52 provincias; sin timestamps futuros;
evict 3-gates sin bypass + delete por entity_ulid; state-machine transiciones; router 13-filas cubre todo;
`trustworthy_needs_independence` (L4) + `chk_trustworthy_needs_quorum` (L2, sobre filas nuevas) bloquean
correctamente.

---

## Orden de ataque €0 restante (post B-1)
1. API serving (H-3/H-4/H-5) — números servidos incorrectos AHORA.
2. F2 TRUNCATE trigger + H-2 migration drift (integridad).
3. Menores €0 (F7 RESOLVED-FK, codes.py province, /health metrics).
El resto (🟡 harvest / ⚪ by-design/data) se ataca en la fase de cosecha o queda declarado.
