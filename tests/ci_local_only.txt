# CI local-only test files — EXCLUDED from the `db-tests` CI job (pytest -m "not unit" --ignore=...).
#
# WHY. The db-tests CI job runs the DB-backed suite against an EPHEMERAL, freshly-migrated, EMPTY
# Postgres (no census). The files below need either the developer's POPULATED cardeep-pg (millions of
# rows) or live network/external-engine access, so they cannot pass on an empty CI DB. They stay part
# of the full local Ferrari run (pre-push) and are verified there.
#
# Empirically derived 2026-06-23 against an empty migrated PG:
#   pytest -m "not unit" --ignore=<each line below>  ->  878 passed / 0 failed.
# Lines starting with '#' and blank lines are ignored by the CI step.
#
# Revisit incrementally: some network files may be fully mocked and CI-safe; promote them out of this
# list once verified to run green offline (the conservative default is to keep them local-only).

# (1) Census-dependent — query the live populated census (pagination / seal / exhaustiveness /
#     canonical entity+inventory / API gaps / response cache / eviction of real dealers):
tests/test_api_pagination.py
tests/test_api_exhaustiveness.py
tests/test_api_seal.py
tests/test_api_canonical.py
tests/test_api_gaps.py
tests/test_api_ratelimit_cache.py
tests/test_evict.py

# (2) Network / external-engine dependent (real fetch cascade / fingerprints / engine licensing /
#     inquisition engines+lenses+prosecutor / quorum precision gate):
tests/test_dealerprobe_concurrent.py
tests/test_engine_license_default.py
tests/test_fetch_cascade.py
tests/test_fingerprints.py
tests/test_inquisition_engines.py
tests/test_inquisition_lenses.py
tests/test_inquisition_prosecutor.py
tests/test_quorum_precision_gate.py
